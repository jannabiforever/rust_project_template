{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.rs) python3 scripts/sort_derives.py \"$f\" ;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
